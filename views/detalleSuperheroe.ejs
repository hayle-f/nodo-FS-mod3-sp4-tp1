<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalles del superhéroe</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <div class="container">
    <h2><%= superheroe.nombreSuperheroe %></h2>
    
    <dl>
      <dt>Nombre Real:</dt>
      <dd><%= superheroe.nombreReal || '-' %></dd>

      <dt>Edad:</dt>
      <dd><%= superheroe.edad || '-' %></dd>

      <dt>Planeta de Origen:</dt>
      <dd><%= superheroe.planetaOrigen || '-' %></dd>

      <dt>Debilidad:</dt>
      <dd><%= superheroe.debilidad || '-' %></dd>

      <dt>Poderes:</dt>
      <dd><%= superheroe.poderes?.join(', ') || '-' %></dd>

      <dt>Habilidad Especial:</dt>
      <dd><%= superheroe.habilidadEspecial || '-' %></dd>

      <dt>Aliados:</dt>
      <dd><%= superheroe.aliados?.join(', ') || '-' %></dd>

      <dt>Enemigos:</dt>
      <dd><%= superheroe.enemigos?.join(', ') || '-' %></dd>
    </dl>

    <div class="acciones-detalle">
      <a href="/api/heroes" class="button cancelar">Volver</a>
      <a href="/api/heroes/editar/<%= superheroe._id %>" class="button">Editar</a>
    </div>
  </div>
</body>
</html>



